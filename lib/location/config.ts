// UK postcode lookup via Postcodes.io. Off by default in OperatorTemplate.
// Set true only for UK platforms that want postcode → coordinate lookup.
// Location and maps still work with operator-entered coordinates.
export const POSTCODE_GEOCODING_ENABLED = false;
